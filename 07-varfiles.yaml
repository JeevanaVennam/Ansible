-name: variables from files
hosts: local
connection: local
vars_files:
  - course.yaml
-tasks
  -name: print course info
  ansible.builtin.debug:
  msg: "Hello, welcome to {{ course }} course, the duration is {{ duration }} and trainer is {{ trainer }}"
  
   
  